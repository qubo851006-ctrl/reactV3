# Test / development-only dependencies.
# Production (requirements.txt) stays free of test tooling.
#
# Install both for local test runs:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Then:
#   python -m pytest tests/ -q

pytest==9.0.3
pytest-cov==7.1.0   # coverage: python -m pytest --cov=. --cov-report=term-missing
ruff==0.15.15       # lint: ruff check .  (config in ruff.toml)
