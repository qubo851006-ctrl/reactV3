# ReactV2 优化建议（V2，面向 20 人部门协作使用）

## 一、总体判断

当前这套 ReactV2 项目已经具备“单人或小范围内部试用”的基础能力，但如果要支撑 **20 人部门日常协作使用**，现阶段最需要优先补强的不是界面美化，而是：

1. **数据安全**
2. **权限隔离**
3. **写入一致性**
4. **可追溯性**

结论可以概括为一句话：

> 这不是“不能用”，而是“可以演示和试运行，但还不适合作为多人长期共用的正式生产工具”。

---

## 二、最关键的缺口（必须优先解决）

### 1. 无用户身份识别 / 无权限控制

目前系统更像“谁能访问页面，谁就能直接操作后端接口”。

风险包括：

- 所有人默认共享同一套能力边界
- 高风险操作（如清空台账）缺少身份确认
- 后续出现误删、误写、误归档时，无法区分是谁操作的

**建议：**

- 增加轻量登录机制（JWT / Session 均可）
- 至少区分 `普通用户` 和 `管理员`
- 高风险操作增加二次确认和权限限制

---

### 2. 并发写入存在覆盖风险，且不止一个模块

当前案件台账、培训台账、授权台账，本质上都还是“读文件 -> 改内存 -> 整体写回”的模式。

这类模式在多人同时使用时，容易出现：

- 后写入的人覆盖前一个人的结果
- 两次操作互相丢失部分更新
- Excel 与 JSON 结果不一致

这类问题比普通功能缺失更危险，因为它直接影响数据可信度。

**建议：**

- 第一阶段优先落地 **SQLite**
- 通过事务保证写入原子性
- 如果暂时不改数据库，至少补上文件锁和串行写入机制

说明：

> 不一定要一开始就上 PostgreSQL。对于当前项目规模，**SQLite 是最低成本、收益最高的第一步**；当多人并发和部署复杂度继续提升，再迁移 PostgreSQL 更合适。

---

### 3. 写入流程不是事务，容易出现“部分成功”

这是比“并发写入”更容易被忽略、但同样很关键的问题。

当前多个流程都存在“一个环节成功、另一个环节失败”的可能，例如：

- JSON 已写入，但 Excel 未成功更新
- 文件已归档，但业务记录未成功写入
- 用户看到成功提示，但底层数据并不完全一致

这种问题的后果是：

- 表面看系统“成功了”
- 实际导出的结果、后续查询、归档文件之间可能对不上

**建议：**

- 将“写记录、写台账、写归档、写日志”设计成统一事务或统一补偿机制
- 至少保证失败时返回明确错误，不要静默吞异常
- 写入成功的定义要从“部分成功”改为“整体成功”

---

### 4. 数据全部保存在服务器本地磁盘，缺乏备份与恢复方案

当前 Excel、JSON、归档文书都在运行机器本地目录下。

这意味着一旦出现以下情况，风险较高：

- 磁盘损坏
- 服务器重装
- 路径误删
- 人为覆盖

**建议：**

- 增加定期自动备份
- 至少保留“按日备份 + 最近 N 版”
- 如有条件，迁移到共享存储或集中式文件服务
- 制定一次“恢复演练”，确保备份真的可用

---

## 三、重要改进（影响日常使用与管理）

### 5. 缺少结构化操作日志 / 审计追踪

目前系统更偏“聊天记录式留痕”，不等于真正的审计日志。

如果未来用于正式协作，建议至少记录：

- 操作人
- 操作时间
- 操作模块
- 新增 / 修改 / 删除内容
- 操作结果（成功 / 失败）

**建议：**

- 单独建立操作日志表
- 高风险操作保留前后变更摘要
- 后续支持按人、按时间、按案件检索

---

### 6. 预览流程已有副作用，取消并不等于“未落库”

从使用体验上看，很多流程像是“先预览，再确认写入”。

但从系统设计上，当前部分流程在确认前就已经执行了归档、文件落盘等动作。这样会带来两个问题：

- 用户以为自己点了取消就什么都没发生
- 实际上文件可能已经进入系统目录

**建议：**

- 将“预览”改成真正无副作用的只读阶段
- 只有用户确认后，才执行落盘、归档、台账写入
- 如果因业务需要必须提前缓存文件，建议放入临时目录，并在取消时自动清理

---

### 7. 台账查看 / 搜索 / 查询界面仍然不足

当前更偏“上传 -> 生成 -> 下载”的工具模式，不够像部门日常协作系统。

20 人部门长期使用时，查询需求通常比录入需求更频繁，尤其需要：

- 查看现有台账
- 搜索案件、培训记录、授权记录
- 按时间、类别、状态筛选
- 打开历史记录进行核对

**建议：**

- 增加台账列表页
- 支持关键词搜索与基础筛选
- 支持查看单条详情
- 后续再考虑“受控编辑”能力

---

### 8. 文件上传和服务端数据校验不足

前端虽然有部分扩展名限制，但真正决定安全性的还是后端校验。

当前风险主要包括：

- 上传错误格式文件
- 上传超大文件导致处理变慢或异常
- 上传内容不符合预期但仍进入处理流程
- 前端可编辑字段缺少严格约束，容易写入脏数据

**建议：**

- 前后端双重校验文件类型和大小
- 对关键字段增加服务端数据模型校验
- 对日期、金额、枚举值做格式约束
- 给用户返回明确、可理解的错误信息

---

### 9. 缺少自动化测试与回归保障

这个项目不是纯静态页面，而是包含：

- 文件上传
- OCR / 文本提取
- AI 结构化抽取
- 台账写入
- Excel 生成

这类流程最怕“改一个地方，另一个流程悄悄坏掉”。

**建议：**

- 补最小可用测试集
- 至少覆盖案件写入、培训写入、授权写入、审计分析四条主链路
- 为关键解析结果准备样例文件和基准输出
- 每次迭代前后跑一轮回归验证

---

## 四、锦上添花（有余力再做）

| 方向 | 建议 |
|------|------|
| 后台任务队列 | AI 处理和文件处理较耗时，建议逐步改为异步任务，避免多人同时提交时前端长时间等待 |
| 通知提醒 | 长任务完成后通过钉钉、企业微信或邮件通知用户 |
| 移动端适配 | 方便领导或同事在手机端查阅记录和结果 |
| 导出格式扩展 | 在 Excel 之外补充 PDF / Word 报告输出 |
| 看板化展示 | 增加案件数量、培训次数、授权记录、审计分类结果等统计视图 |

说明：

> “任务队列”建议保留为方向，不必在当前阶段一上来就限定为 Celery。先明确异步任务机制即可。

---

## 五、推荐优先级

如果只能先做一件事，优先级不是“做一个更漂亮的列表页”，而是先把 **数据安全和一致性** 兜住。

推荐落地顺序：

1. **统一数据存储与事务机制**  
   优先引入 SQLite，解决并发覆盖、部分成功、JSON/Excel 不一致问题

2. **登录 + 权限控制**  
   建立最基本的多人协作边界

3. **结构化操作日志**  
   让所有关键操作可追溯

4. **真正的预览/确认两阶段流程**  
   避免“取消了但其实已经落盘”的体验和数据问题

5. **台账列表与搜索界面**  
   提升日常使用效率

6. **上传校验 + 数据模型校验 + 自动化测试**  
   让系统更稳、更可维护

---

## 六、最终结论

这份项目优化方向总体是 **真实、准确、而且抓到了重点** 的。

如果面向 20 人部门协作，我的判断是：

- 这套系统**最值得优先投入的不是新功能，而是底层治理**
- 当前最大风险不是“功能还不够多”，而是“多人使用后数据是否可信、是否可追溯、是否可恢复”
- 只要先补上 **事务、权限、日志、校验** 这四个基础能力，这个项目就能从“试用工具”明显进化成“可持续使用的内部系统”

---

*修订日期：2026-04-28*
